#!/bin/bash

set -ex

# Generate aliaswallet.conf
aliaswallet_init

if [ $# -gt 0 ]; then
    args=("$@")
else
#    args=("-rpcallowip=::/0")
    args=("-rpcallowip=*")
fi

exec aliaswalletd "${args[@]}"
